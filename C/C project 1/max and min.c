int max(int x, int y)
{
	int z;
	if (x > y)
	{
		z = x;
	}
	else if (x == y)
	{
		z = x;
	}
	else
	{
		z = y;
	}
	return z;
}

int min(int x, int y)
{
	int z;
	if (x < y)
	{
		z = x;
	}
	else if (x == y)
	{
		z = x;
	}
	else
	{
		z = y;
	}
	return z;
}
